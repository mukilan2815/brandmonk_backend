# Server Port
PORT=5000

# MongoDB Connection String
# Replace with your MongoDB Atlas connection string or local MongoDB URI
MONGO_URI=mongodb://localhost:27017/brandmonk_academy

# Email Configuration (for sending certificate emails)
# Use Gmail App Password for EMAIL_PASS
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Frontend URL (for certificate links in emails)
FRONTEND_URL=http://localhost:3000
